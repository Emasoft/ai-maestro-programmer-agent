[project]
name = "ai-maestro-programmer-agent"
version = "1.0.24"
description = "Programmer Agent plugin for AI Maestro"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["pyyaml"]

[tool.ruff]
line-length = 320

[tool.mypy]
ignore_missing_imports = true
